# Core ML stack
numpy
matplotlib
scikit-learn
scikit-image
h5py
numba

# Deep learning
# Note: The code mixes `keras` and `tensorflow.keras` imports.
# Use a TensorFlow 2.x + Keras <3 pairing to avoid incompatibilities.
# Deep learning
# Note: The code mixes `keras` and `tensorflow.keras` imports.
# Use a TensorFlow 2.x + Keras <3 pairing to avoid incompatibilities.

# For Intel/Linux/Windows, use:
# tensorflow
# keras<3

# For Apple Silicon (M1/M2/M3), use:
tensorflow-macos
tensorflow-metal
keras<3

# Imaging & I/O
opencv-python
Pillow
SimpleITK
nibabel
# pynrrd provides the `nrrd` module
pynrrd

# Data utilities
split-folders

# Optional (used by predict_resnet_2d.py)
# qubvel/segmentation-models depends on specific TF/Keras versions; adjust if needed.
segmentation-models==1.0.1
